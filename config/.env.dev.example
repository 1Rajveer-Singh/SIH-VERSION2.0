# Development Environment Variables
# Copy this file to .env.dev for development

# Database Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=dev_password
MONGO_DATABASE=rockfall_prediction_dev

# Redis Configuration
REDIS_PASSWORD=dev_redis_password

# Application Security (Development only - not secure)
SECRET_KEY=dev-secret-key-not-for-production-use-only
JWT_SECRET_KEY=dev-jwt-secret-key-not-for-production-use

# Email Configuration (Optional for development)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# SMS Configuration (Optional for development)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=dev_password

# Application Configuration
MAX_UPLOAD_SIZE=50MB
LOG_LEVEL=DEBUG
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001